name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  testing:
    name: Run Laravel Testing
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: echo Just testing GitHub actions